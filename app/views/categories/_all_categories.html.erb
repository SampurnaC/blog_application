<div class="col-md-2">
  <h2>Categories</h2>
  <% @categories.each do |category| %>
    <p><%= link_to "#{category.name}", category_path(category), class: "btn btn-light" %></p>
    <hr>
  <% end %>       
</div>
